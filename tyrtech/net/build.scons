Import('env')
Import('BUILD_DIR')


net_sources = [
    'socket_channel.cpp',
    'socket_reader.cpp',
    'socket_writer.cpp',
    'uri.cpp'
]

env.StaticLibrary(target='{0}/net'.format(BUILD_DIR), source=net_sources)
